global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'medical_classifier_monitor'

scrape_configs:
  - job_name: 'medical_classifier_api'
    static_configs:
      - targets: ['api:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s
